

//Cruz Garcia Misael Calculo vectorial 2TM25//


#include <iostream>
include <cmath>

#include <iostream>
#include <stdio.h>
#include <iostream>

#define PI  3.14159
using namespace std;

float x=0; 
float y=0;
float teta=0;
float r=0;
float X=0,Y=0,R=0;
float TETA=0;
float r=0;
float rad =0;
float p1p2=sqrt(((x2-x1)*(x2-x1))+((y2-y1)*(y2-y1))+((z2-z1)*(z2-z1)));
float grad=0;


//con esta linea ya ni funciona
std::cout<<"si quieres convertir coordenadas rectangulares a polares presiona 1 y si quieres convertir polares a rectangulares presiona 2: ";
	std::cin>>a;
	if(a==1)
	{
	std::cout<< "coordenadas rectangulares a polares: "<<endl;
	std::cout << "introduce x: ";
	std::cin>>x;
	std::cout<<"introduce y: ";
	std::cin>>y;
	std::cout<<"r es igual a: ";

int esfericasacilindricas( float r, float teta, float P
double w1,w2,w3;
int upuntov(double U1, double U2,double U3, double V1, double V2, double V3){
	w1=U1+V1;
	w2=U2+V2;
	w3=U3+V3;
return 0;

int recapol(float x=0, float y=0){
	
	r=sqrt((x*x)+(y*y));
	teta=atan(y/x);
	cout<<"("<<r<<","<<teta<<")";
return 0;

//distancia entre puntos//

	int disp1p2(float x1=0, float y1=0, float z1=0, float x2=0, float y2=0, float z2=0)

return 0;
}


int gradarad(float gra){
		rad=(gra* PI)/180;
		cout<<rad;
return 0; 
}

int radagrad (float rad){
	float gra=0; 
	gra=(rad*180)/PI;
	cout<<gra;
return 0; 
}






{
	
	r=sqrt( (x*x)+(y*y));
    std::cout<<r<<endl;
    cout<<"Teta es igual a: ";
    teta=atan((y/x));
    cout<<teta;
	} 
	else{
    cout<<"coordenadas polares a rectangulares "<<endl;
    cout << "introduce r: ";
    cin>>R;
    cout << "introduce teta: ";
    cin>>TETA;
    cout << "x es igual a: ";
    X=R*cos(TETA);
    cout<<X<<endl;
    cout << "y es igual a: ";
    Y=R*sin(TETA);
    cout<<Y;
	}
	return 0;
	
}



	
	x=r*sin(phy)*cos(teta);
	
	cout<<x<<","<<y<<","<<z<<end;
	
	
return 0;
}


    
using namespace std; 
	
int main () {
		
		
		
		rad=(grad*PI)/180;
		
		cout<<grad<<endl; 
}


	
using namespace std;
	
int main(){
	w=(U1*V1)+(U2*V2)+(U3*V3);
		
	cout<<","<<w2<<","<<w3<<endl;
}	
	
cout<<p1p2;

return 0;

}
	
using namespace std;

//el main va en otra parte
int main() {
		
		x= r cos (teta);
		y= r sin (teta);
		
		cout<<x <<" " <<y;	

		
return 0;
}

//Cruz Garcia Misael Calculo vectorial 2TM25 tiene muchos errores//

//profe le vuelve a subir los códigos ya corregidos, espero y pueda 
Revisarlos de nuevo. Bonita noche//
//NO BORRE EL COMENTARIO QUE ME HIZO //
